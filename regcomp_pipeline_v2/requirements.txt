# Regulation & Compliance Corpus Pipeline v0.9 - Requirements (adapted from chemistry)
-r ../requirements.constraints.txt

# Optional: for accurate token counting in catalog_builder
# tiktoken>=0.5.0

# Optional: for parallel processing improvements
# concurrent-futures (included in Python 3.2+)

# v0.9: Optional for Parquet output

# v0.9: Optional for near-duplicate detection
# datasketch>=1.6.0

# v0.9: Optional for InChIKey/SMILES normalization
# rdkit>=2023.9.0
