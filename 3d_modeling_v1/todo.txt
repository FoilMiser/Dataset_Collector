================================================================================
TODO — 3D Modeling / 3D Printing Corpus Pipeline
================================================================================

Legend:
  [x] = Completed
  [~] = Partially addressed / MVP landed
  [ ] = Not yet implemented

================================================================================
MVP COMPLETED (v1.0 bootstrap)
================================================================================
  [x] Targets manifest adapted for 3D (permissive/record-level/copyleft split)
  [x] License/denylist/field schemas cloned and updated for meshes and printing
  [x] Wrapper script + pipeline driver reused from chemistry prototype
  [x] Requirements file documented with optional mesh/crawler/S3 deps

================================================================================
NEXT UP (immediate sprint)
================================================================================
  [x] Land `s3_public` downloader with listing snapshots + etag capture; wire to smithsonian_open_access_3d
  [x] Land `web_crawl` downloader with robots/allowlist globs + ToS snapshot; plumb defaults into targets.yaml resolver entry
  [x] Add `mesh_worker.py` scaffolding and CLI flags; emit mesh_record_v1.0.0 JSONL + thumbnails/point clouds stubs
  [x] Implement record-level filter pipeline for Thingi10K/Objaverse: map license -> SPDX, enforce allowlist, drop NC/ND/NoAI
  [x] Add weapon/trademark filter hook (regex from denylist + placeholder model call) in mesh_worker pipeline

================================================================================
HIGH PRIORITY (next)
================================================================================
  [ ] Harden mesh_worker validation (unit inference, watertight checks, scale normalization)
  [ ] Geometry-aware dedupe (light-field/spherical harmonic descriptors)
  [ ] Printing profile + gcode generation for a safe subset; emit printing_profile_v1.0.0
  [ ] Parquet output for mesh metadata (zstd) and global stats collection
  [ ] Attribution bundle generator for CC-BY sources (per-target)
  [ ] Sample notebooks: inspect meshes, review thumbnails, license QA reports

================================================================================
MEDIUM PRIORITY
================================================================================
  [ ] Crawling/test harness with small fixtures for s3_public + web_crawl
  [ ] Incremental manifest diff + delta-only mesh normalization runs
  [ ] Staging/pools size guardrails with alerting

================================================================================
LOWER PRIORITY / FUTURE
================================================================================
  [ ] CI smoke tests with small 3D fixtures
  [ ] Docker/compose + Helm templates for job execution
  [ ] UI/dashboard for YELLOW review (license + weapon/IP flags)
  [ ] Metrics/alerts for download/mesh validation throughput

================================================================================
NOTES
================================================================================
- Treat community uploads as record-level; default deny NC/ND/NoAI and weaponized/IP-rip content.
- Snapshot Terms/ToS and license evidence before downloads; keep catalogs and manifests auditable.
- Keep copyleft outputs segregated; produce attribution bundles for CC-BY sources.
