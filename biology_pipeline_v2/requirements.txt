# Biology Corpus Pipeline v2.0.0 - Requirements
-r ../requirements.constraints.txt

# Optional: for accurate token counting in catalog_builder
# tiktoken>=0.5.0

# Optional: for parallel processing improvements
# concurrent-futures (included in Python 3.2+)

# Optional: for near-duplicate detection
# datasketch>=1.6.0

# Optional: for PDF extraction with figure/table retention
# pypdf>=4.0.0
# pdfminer.six>=20221105

# Optional: for domain-specific detection heuristics
# sympy>=1.12  # or other parsing helpers as needed
