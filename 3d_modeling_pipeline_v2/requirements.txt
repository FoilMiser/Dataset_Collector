# 3D Modeling Corpus Pipeline v2.0 - Requirements
# Core dependencies
pyyaml>=6.0
requests>=2.32.2,<2.33
datasets>=2.20.0,<2.21
pyarrow>=15.0.0
jsonschema>=4.22.0

# Optional: public S3 support (s3_public strategy)
boto3>=1.34.0
# aiobotocore>=2.12.3

# Optional: polite web crawling for curated endpoints
# httpx>=0.27.0
# beautifulsoup4>=4.12.0
# selectolax>=0.3.17

# 3D mesh processing (recommended for mesh_worker)
# trimesh>=4.0.0
# meshio>=5.3.0
# open3d>=0.18.0
# pillow>=10.2.0

# Optional: for accurate token counting in catalog_builder
# tiktoken>=0.5.0

# Optional: for geometry/text near-duplicate detection
# datasketch>=1.6.0
