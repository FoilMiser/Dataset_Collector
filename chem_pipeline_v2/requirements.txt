# Chemistry Corpus Pipeline v2 - Requirements
# Core dependencies
pyyaml>=6.0
requests>=2.31.0

# Optional: for HuggingFace datasets strategy
# datasets>=2.20.0

# Optional: for accurate token counting in catalog_builder
# tiktoken>=0.5.0

# Optional: for parallel processing improvements
# concurrent-futures (included in Python 3.2+)

# Optional: for Parquet output
# pyarrow>=14.0.0

# Optional: for near-duplicate detection
# datasketch>=1.6.0

# Optional: for PDF extraction with scientific/chemical content retention
# pypdf>=4.0.0
# pdfminer.six>=20221105

# Optional: for scientific markup detection heuristics
# sympy>=1.12
