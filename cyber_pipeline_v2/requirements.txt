# Cybersecurity Corpus Pipeline v2 - Requirements
-r ../requirements.constraints.txt

# Optional: for accurate token counting in catalog_builder
# tiktoken>=0.5.0

# Optional: for parallel processing improvements
# concurrent-futures (included in Python 3.2+)

# Optional: for Parquet output

# Optional: for near-duplicate detection
# datasketch>=1.6.0
#
# Domain-specific enrichments (optional, install as needed)
# networkx>=3.2.0    # useful for STIX relationship graph exports
