# Code Corpus Pipeline v2 - Requirements
-r ../requirements.constraints.txt

# Optional: for accurate token counting in catalog_builder
# tiktoken>=0.5.0

# Optional: for parallel processing improvements
# concurrent-futures (included in Python 3.2+)

# Optional: for near-duplicate detection
# datasketch>=1.6.0

# Optional: for repository handling in code_worker (alternative to git CLI)
# gitpython>=3.1.40

# Optional: for secrets detection
# detect-secrets>=1.4.0

# Optional: for charset detection on edge-case files
# charset-normalizer>=3.3.2
